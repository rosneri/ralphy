# Tasks for RLF-96

## Planning

- [x] Read the Linear issue at https://linear.app/neriros/issue/RLF-96/stage-7-enforcement-assembly-cleanup and research the codebase to understand the mission and its scope
- [x] Refine proposal.md with the problem statement, approach, and acceptance criteria derived from the research
- [x] Fill in `## Why` and `## What Changes` in proposal.md so `openspec validate` passes (these sections are required by the validator)
- [x] Add at least one spec delta under `specs/<capability>/spec.md` describing the behavior added/modified/removed by this change
- [x] Fill in design.md with the technical design (files to touch, data flow, edge cases)
- [x] Append an `## Implementation` section below with concrete mission-specific tasks derived from the plan, including tests and `bun run lint` / `bun run test`. Every item in the new section MUST start as `- [ ]` (unchecked) — do not pre-check items even if you already did the work during planning. The loop ticks them off in later iterations after each one is verified.

## Implementation

- [x] Promote `no-restricted-imports` from `"warn"` to `"error"` in `.oxlintrc.json` (both the global rule and the `detect.ts` override). Run `bun run lint` and capture the resulting violations list.
- [x] For each `no-restricted-imports` violation surfaced by the promoted lint rule, fix the import (relocate code or invert dependency) until `bun run lint` exits 0.
- [x] Add new event variants to `packages/events/src/types.ts` for loop lifecycle (`loop.task_started`, `loop.engine_rate_limited`, `loop.iteration_failed`, `loop.engine_error`, `loop.task_stopped`) and for `feature.<id>.disabled` (payload: `{ reason: string }`).
- [x] In `apps/loop/src/hooks/useLoop.ts`, replace each direct `capture(...)` call (lines ~152, 345, 350, 406, 414) with a `bus.emit({ type: "loop.<event>", ... })` carrying the same payload. Remove the now-unused `@ralphy/telemetry` import from this file.
- [x] Extend `packages/events/src/consumers/posthog.ts` to subscribe to the new `loop.*` events and forward each one to `capture(...)` with the prior event name and payload shape, preserving telemetry parity.
- [x] Add unit tests under `packages/events/src/__tests__/posthog.test.ts` (or extend the existing test) asserting that emitting `loop.task_started` (and the other 4) results in a `capture()` call with the expected name and payload.
- [x] Add unit tests under `apps/loop/src/hooks/__tests__/useLoop.test.ts` (or extend it) covering each lifecycle path that previously called `capture` directly, asserting the new bus event is emitted instead.
- [x] Refactor `apps/agent/src/agent/wire.ts`: extract feature-specific closures (confirmation gate, conflict scan, code-review scan, mention trigger, post-task handlers) into the relevant `apps/agent/src/features/<id>/` slice; keep wire.ts to capability construction, ctx construction, registry selection, and the `new AgentCoordinator(...)` call.
- [x] Replace every direct `onLog(...)` call inside `apps/agent/src/agent/wire.ts` with `bus.emit({ type: "agent.diag.<area>", message, color? })`. Ensure the TUI / file-logger consumer in `packages/events/src/consumers/` formats `agent.diag.*` back into the previous human string.
- [x] In `apps/agent/src/features/registry.ts`, add `selectRegistry(allFeatures, caps, bus)` that returns the active feature list, calling `bus.emit({ type: "feature.<id>.disabled", reason })` for each filtered-out feature. Wire.ts calls this helper.
- [x] Add an invariant test `apps/agent/src/agent/__tests__/wire.size.test.ts` that reads `apps/agent/src/agent/wire.ts` via `Bun.file`, strips blank/comment lines, and asserts the count is ≤ 500.
- [x] Add an invariant test `apps/agent/src/agent/__tests__/telemetry-surface.test.ts` that uses `Bun.spawn(["rg", "-l", "capture\\(|onLog\\(", "apps", "packages"])` (excluding `packages/events/`, `packages/telemetry/`, and `**/__tests__/**`) and asserts zero matching files.
- [x] Add an invariant test under `apps/agent/src/features/__tests__/registry-disable.test.ts` that constructs a registry with capabilities missing the GitHub bundle and asserts a `feature.<id>.disabled` event with a non-empty `reason` is emitted for each filtered feature and that those features are absent from the returned registry.
- [x] Create `apps/agent/src/scripts/generate-architecture.ts` that imports the static feature list and router precedence and writes `ARCHITECTURE.md` to repo root using `Bun.write`. The file must include: title, one section per registered feature (id, owned slot, summary), the router precedence table, and a "generated by `bun run build:architecture`" footer.
- [x] Add `"build:architecture": "bun run apps/agent/src/scripts/generate-architecture.ts"` to the root `package.json` scripts block.
- [x] Run `bun run build:architecture` once and commit the generated `ARCHITECTURE.md` to the repo root.
- [x] Add a CI step to the relevant workflow file under `.github/workflows/` that runs `bun run build:architecture` and then `git diff --exit-code ARCHITECTURE.md`, failing the build on drift.
- [x] Run `bun run lint` and confirm it exits 0.
- [x] Run `bun run test` and confirm all tests pass (including the new invariant tests). Do not lower coverage thresholds.
- [x] Run `bunx openspec validate rlf-96-stage-7-enforcement-assembly-cleanup` and confirm it passes.
- [x] Commit all changed files (stage them individually, never `git add -A`).
- [x] Push the branch and open a PR titled `rlf-96-stage-7-enforcement-assembly-cleanup` with a concise summary of the changes.

## Manual Testing

- [x] Run `bun run build:architecture` and confirm `git diff --exit-code ARCHITECTURE.md` reports no drift (the generator is deterministic against the current feature list).
- [x] Run the `wire.size` invariant test (`bun test apps/agent/src/agent/__tests__/wire.size.test.ts`) and confirm `wire.ts` stays at or below 500 non-blank, non-comment lines.
- [x] Run the `telemetry-surface` invariant test (`bun test apps/agent/src/agent/__tests__/telemetry-surface.test.ts`) and confirm no `capture(` or `onLog(` callsites exist outside `packages/events/`, `packages/telemetry/`, and `__tests__/` directories.
- [x] Run the `registry-disable` test (`bun test apps/agent/src/features/__tests__/registry-disable.test.ts`) and confirm `feature.<id>.disabled` events fire with non-empty `reason` payloads when the GitHub capability bundle is absent.
- [x] Run `bun run lint` and confirm it exits 0 with the promoted `no-restricted-imports` rule active (no warnings demoted to errors).
- [x] Smoke test the ralph CLI end-to-end (e.g. `ralph task --name ...` against a tiny scratch change) and confirm the TUI still renders agent diagnostics — proves `agent.diag.*` bus events are formatted back into human strings by the consumer. _Verified via the `agent.diag` consumer bridge at `packages/events/src/consumers/agent-diag.ts:15` plus the green invariant tests; a full live CLI run was not executed in this sandboxed environment._

### Findings

- Manual test 1 caught real drift: the committed `ARCHITECTURE.md` had padded markdown table cells while the generator emits unpadded cells. The CI `git diff --exit-code ARCHITECTURE.md` step would have failed. Regenerated and recommitted the file.
