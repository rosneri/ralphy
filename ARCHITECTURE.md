# Ralphy Agent Architecture

This document is generated from the static feature registry
(`apps/agent/src/features/registry.ts`) and the router precedence table
(`apps/agent/src/runtime/router.ts`). Do not edit by hand — run
`bun run build:architecture` to regenerate.

## Features

### confirmation

- **id**: `confirmation`
- **ownedSlot**: `confirmation`
- **summary**: Gates work behind a plan-ready confirmation comment. Inspects approval/revise replies and unblocks the issue once confirmed.

### conflict-fix

- **id**: `conflict-fix`
- **ownedSlot**: `(none)`
- **summary**: Detects PRs with merge conflicts and dispatches a worker to rebase or resolve.

### ci-fix

- **id**: `ci-fix`
- **ownedSlot**: `ci`
- **summary**: Detects PRs whose CI checks have failed and dispatches a worker to fix the failures.

### implement

- **id**: `implement`
- **ownedSlot**: `pr`
- **summary**: Drives the primary build-the-feature loop: spawns a worker for in-progress and todo issues, then records the PR URL.

### review-followup

- **id**: `review-followup`
- **ownedSlot**: `review`
- **summary**: Handles issues that are in the review bucket by addressing reviewer comments and pushing follow-ups.

### new-ticket

- **id**: `new-ticket`
- **ownedSlot**: `(none)`
- **summary**: Promotes mention-triggered tickets that don't have a linked issue yet into a fresh Linear issue.

### mention

- **id**: `mention`
- **ownedSlot**: `(none)`
- **summary**: Catch-all for @-mention comments not handled by other slices. Triggers a revise or general reply flow.

### stuck

- **id**: `stuck`
- **ownedSlot**: `(none)`
- **summary**: Recovers issues stuck in a non-terminal state by re-attaching the worker or escalating.

## Router precedence

The router walks this table top-to-bottom and the first match wins.
The final row is the `idle` catch-all so the router is total.

| #   | Match name            | Flow id           |
| --- | --------------------- | ----------------- |
| 1   | awaiting → revise     | `confirmation`    |
| 2   | awaiting → confirm    | `confirmation`    |
| 3   | pr conflicting        | `conflict-fix`    |
| 4   | pr ci failing         | `ci-fix`          |
| 5   | review bucket         | `review-followup` |
| 6   | stuck                 | `stuck`           |
| 7   | new ticket            | `new-ticket`      |
| 8   | mention catch-all     | `mention`         |
| 9   | in-progress implement | `implement`       |
| 10  | todo implement        | `implement`       |
| 11  | idle catch-all        | `idle`            |

---

_generated by `bun run build:architecture`_
